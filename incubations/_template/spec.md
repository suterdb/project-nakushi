# Spec — <INCUBATION_NAME>

This file is the single consolidated reference for building the MVP.
It synthesizes `problem.md` + `mvp-scope.md` + the ADR set into one place.

Bar for "done": someone who has NOT read `problem.md`/`mvp-scope.md`/the ADRs
must be able to build against this file alone. Do not write "see mvp-scope.md" —
restate the relevant content directly.

## One-line summary
(What is this product, in one sentence.)

## Screens
| Screen | Purpose | Key elements |
|---|---|---|
| | | |

## API list
| Method | Path | Purpose | Request | Response |
|---|---|---|---|---|
| | | | | |

## Data model (summary)
(Entities, key fields, relationships — enough to start a schema, not the full DDL.)

## Out-of-scope (restated from mvp-scope.md)
-

## Open technical questions
(Anything not yet decided that implementation-plan.md will need to resolve.)
-
